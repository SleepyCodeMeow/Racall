# Development only. Desktop generates its own ephemeral session token.
OPENNOTEBOOK_SESSION_TOKEN=replace-with-a-random-token-at-least-24-characters
# Optional process-only override; prefer the OS keychain in Settings.
# OPENNOTEBOOK_API_KEY=
# Optional alternate desktop data directory.
# OPENNOTEBOOK_DATA_DIR=
